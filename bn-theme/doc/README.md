# 文档说明

本目录用于存放 **主题相关的补充说明**（与 `bn-hex-comment-fix` 等仓库中的 `doc/` 用法一致）。

当前仓库根目录仅包含 **`ida-classic-light.bntheme`**，没有单独的 Markdown 长文；**安装与使用说明**请直接看上一级目录的 [README.md](../README.md)。

若你日后补充例如：

- 与 IDA 默认浅色的对照表；
- 自定义 `theme-colors` 字段的备忘；

可将对应 `.md` 文件放在本目录下，并在此 `README.md` 里增加条目链接。
